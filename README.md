# Image-Processing
Developed a program which takes an (.BMP Format) image file from user and generate another .BMP File.
This BMP file format used to store bitmap digital image.
It replace the blue colour with red colour in the image.
